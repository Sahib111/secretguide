<!-- function validate(){	if(document.settings.keywordfile.value=="")	{		alert("Please upload Keyword File!");		document.settings.keywordfile.focus();		return false;	}	if(document.settings.initnumpages.value=="")	{		alert("Please set the initial number of pages to build!");		document.settings.initnumpages.focus();		return false;	}    if(document.settings.cronnumpages.value=="")	{		alert("Please set the cron number of pages to build!");		document.settings.cronnumpages.focus();		return false;	}		if(document.settings.title.value=="")	{		alert("Please enter Title!");		document.settings.title.focus();		return false;	}	if(document.settings.description.value=="")	{		alert("Please enter Description!");		document.settings.description.focus();		return false;	}	if(document.settings.url.value=="")	{		alert("Please enter Sponsored URL!");		document.settings.url.focus();		return false;	}	if(document.settings.indname.value=="")	{		alert("Please enter Index Name!");		document.settings.indname.focus();		return false;	}	if(document.settings.keywords.value=="")	{		alert("Please enter number of Keywords per page!");		document.settings.keywords.focus();		return false;	}	else if(!parseInt(document.settings.keywords.value)||(document.settings.keywords.value<1))	{		alert("Please enter correct number of Keywords per page!");		document.settings.keywords.focus();		return false;	}    if(!parseInt(document.settings.minresults.value) ||document.settings.minresults.value == "" || parseInt(document.settings.minresults.value) < 1)	{		alert("Please enter valid number of Min Results to extract per page !");		document.settings.minresults.focus();		return false;	}	if(!parseInt(document.settings.results.value) || document.settings.results.value == "" || parseInt(document.settings.results.value) < 1)	{		alert("Please enter valid number of Max Results to extract per page !");		document.settings.results.focus();		return false;	}		if(parseInt(document.settings.results.value) < parseInt(document.settings.minresults.value))	{		alert("Min Results could not be higher than Max Results");		document.settings.minresults.focus();		return false;	}		if(document.settings.bgcolor.value=="")	{		alert("Please enter Background Color!");		document.settings.bgcolor.focus();		return false;	}	if(document.settings.homepageurl.value=="")	{		alert("Please enter Homepage URL!");		document.settings.homepageurl.focus();		return false;	}	if(document.settings.sitemapname.value=="")	{		alert("Please select Output Directory!");		document.settings.sitemapname.focus();		return false;	}return true;}function validate1(){	if(document.settings.host.value=="")	{		alert("Please enter Host name!");		document.settings.host.focus();		return false;	}	if(document.settings.email.value=="")	{		alert("Please enter E-mail Address!");		document.settings.email.focus();		return false;	}	if(document.settings.emailsperday.value=="")	{		alert("Please enter number of E-mails Per Day!");		document.settings.emailsperday.focus();		return false;	}	document.settings.action="install.php";	document.settings.submit();}//-->